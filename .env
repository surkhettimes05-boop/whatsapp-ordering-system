# Docker Compose Environment Variables

# PostgreSQL Settings
DB_USER=postgres
DB_PASSWORD=supersecretpassword123
DB_NAME=whatsapp_ordering
DB_PORT=5432

# Redis Settings
REDIS_PASSWORD=supersecretredispassword456
REDIS_PORT=6379

# Backend API Settings
PORT=5000
JWT_SECRET=a_very_long_and_secure_jwt_secret_that_is_at_least_32_characters_long
WHATSAPP_VERIFY_TOKEN=a_secure_token_for_whatsapp_verification
WHATSAPP_API_KEY=your_whatsapp_api_key_here

# Note: For a real production environment, these secrets should be
# managed by a secrets manager (e.g., HashiCorp Vault, AWS Secrets Manager)
# and not be hardcoded in a .env file.
DATABASE_URL=postgresql://postgres:supersecretpassword123@localhost:5432/whatsapp_ordering
