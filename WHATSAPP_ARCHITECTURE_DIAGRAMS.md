# WhatsApp Production Architecture & Flows

**Date**: January 22, 2026

---

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        PRODUCTION SYSTEM                         │
└─────────────────────────────────────────────────────────────────┘

Your Application (Backend)
    │
    ├── REST API
    │   ├── POST /api/v1/whatsapp/send           (outgoing messages)
    │   ├── GET  /api/v1/whatsapp/delivery-status  (check status)
    │   └── GET  /api/v1/whatsapp/delivery-metrics (get metrics)
    │
    ├── Message Queue (Redis + Bull)
    │   ├── Queued messages
    │   ├── Worker processes (configurable concurrency)
    │   └── Retry logic for failures
    │
    ├── Database (PostgreSQL)
    │   ├── WhatsAppMessage (message records)
    │   ├── MessageStatusLog (status transitions)
    │   └── MessageDeliveryMetrics (analytics)
    │
    └── Webhook Receivers
        ├── POST /api/v1/whatsapp/webhook   (incoming messages)
        └── POST /api/v1/whatsapp/status    (status callbacks)

        (Twilio pushes data here)

                    ↕ HTTPS

┌─────────────────────────────────────────────────────────────────┐
│                      TWILIO CLOUD                                │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  WhatsApp Business API (Production)                      │  │
│  │                                                          │  │
│  │  Account: ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx    │  │
│  │  Business Number: +1 (234) 567-8900                     │  │
│  │                                                          │  │
│  │  Webhook Endpoints:                                     │  │
│  │  - Incoming: https://api.yourdomain.com/webhook         │  │
│  │  - Status:   https://api.yourdomain.com/status          │  │
│  │                                                          │  │
│  │  Status Callback: Enabled ✅                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                            ↕                                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  WhatsApp Server                                         │  │
│  └──────────────────────────────────────────────────────────┘  │
│                            ↕                                    │
└─────────────────────────────────────────────────────────────────┘

                    ↕ WhatsApp API

┌─────────────────────────────────────────────────────────────────┐
│                    WHATSAPP NETWORK                              │
│                                                                  │
│  User's Phone ←→ WhatsApp Client ←→ WhatsApp Servers            │
│                                                                  │
│  Message received ✅                                             │
│  Message delivered ✅                                            │
│  Message read ✅                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Message Flow Sequence

### Outgoing Message Flow

```
┌─────────────────┐
│  Your App Code  │
└────────┬────────┘
         │ whatsappService.sendWhatsAppMessage(to, text)
         ↓
┌─────────────────────────────────┐
│  WhatsApp Service Handler       │
│  (src/services/whatsapp.service)│
└────────┬────────────────────────┘
         │ Format number, validate
         ↓
    Use Queue? (Production)
    ├─ YES (queue service) ──────────┐
    │                                 │
    └─ NO (immediate send) ────────┐ │
                                   │ │
    ┌──────────────────────────────┘ │
    │ Queue Message (Redis)          │
    │ Priority: 5 (configurable)     │
    ├──────────────────────────────┬─┘
    │                              │
    ↓                              │
┌──────────────────┐               │
│ Worker Process   │               │
│ (processes queued│               │
│  messages at     │               │
│  configurable    ↓               ↓
│  concurrency)    ↓               ↓
└────────┬─────────────┬───────────┘
         │             │
         ↓             ↓
    ┌─────────────────────────────────┐
    │  sendMessageImmediate()         │
    │  (Twilio API call)              │
    └────────┬────────────────────────┘
             │ client.messages.create({
             │   body: message,
             │   from: TWILIO_WHATSAPP_FROM,
             │   to: recipientNumber
             │ })
             ↓
    ┌─────────────────────────────────┐
    │  Twilio API Response            │
    │  {                              │
    │    sid: "SMxxxxxxxxxx",         │
    │    status: "queued",            │
    │    ...                          │
    │  }                              │
    └────────┬────────────────────────┘
             │
             ↓
    ┌─────────────────────────────────┐
    │  Log to Database                │
    │  - messageSid                   │
    │  - to, from, body               │
    │  - status = "queued"            │
    │  - direction = "OUTGOING"       │
    │  - createdAt = now()            │
    └────────┬────────────────────────┘
             │
             ↓
    ┌─────────────────────────────────┐
    │  Return Result to API           │
    │  {                              │
    │    success: true,               │
    │    messageSid: "SMxxxxxxxxxx",  │
    │    status: "queued"             │
    │  }                              │
    └─────────────────────────────────┘
```

### Incoming Message Flow

```
┌─────────────────────────────────┐
│  User sends WhatsApp message    │
│  to business number             │
└────────┬────────────────────────┘
         │
         ↓
    WhatsApp Network
         │
         ↓
    ┌─────────────────────────────────┐
    │  Twilio receives message        │
    └────────┬────────────────────────┘
             │ Twilio sends webhook POST
             ↓ to WEBHOOK_URL
    ┌─────────────────────────────────┐
    │  Your Webhook Endpoint          │
    │  POST /api/v1/whatsapp/webhook  │
    │  {                              │
    │    From: "whatsapp:+xxxx...",   │
    │    To: "whatsapp:+yyyy...",     │
    │    Body: "User message",        │
    │    MessageSid: "SMxxxxxxxxxx"   │
    │  }                              │
    └────────┬────────────────────────┘
             │ HTTP 200 OK (immediate)
             ↓ (Don't make user wait!)
    ┌─────────────────────────────────┐
    │ Message Processing (Async)      │
    │ (happens in background)         │
    └────────┬────────────────────────┘
             │
             ├─ Parse message
             ├─ Create/update user
             ├─ Log to database
             ├─ Trigger handlers
             │  (order creation,
             │   catalog request, etc.)
             ├─ Send response (optional)
             └─ Log analytics
```

### Status Callback Flow

```
┌──────────────────────────────────────┐
│  Twilio receives status update       │
│  from WhatsApp servers               │
│  (sent, delivered, read, failed)     │
└────────┬─────────────────────────────┘
         │
         ↓ Signature validation (HMAC-SHA1)
    ┌──────────────────────────────────┐
    │  POST /api/v1/whatsapp/status    │
    │  {                               │
    │    MessageSid: "SMxxxxxxxxxx",   │
    │    MessageStatus: "delivered",   │
    │    From: "whatsapp:...",         │
    │    To: "whatsapp:...",           │
    │    X-Twilio-Signature: "..."     │
    │  }                               │
    └────────┬─────────────────────────┘
             │ Return 200 OK immediately!
             ↓ (Async processing)
    ┌──────────────────────────────────┐
    │  Find message in database        │
    │  by messageSid                   │
    └────────┬─────────────────────────┘
             │
             ↓
    ┌──────────────────────────────────┐
    │  Check if status changed         │
    │  (queued → sent → delivered)     │
    └────────┬─────────────────────────┘
             │ YES
             ↓
    ┌──────────────────────────────────┐
    │  Log transition to                │
    │  MessageStatusLog table           │
    │  - messageSid                     │
    │  - previousStatus                 │
    │  - newStatus                      │
    │  - statusChangedAt = now()        │
    └────────┬─────────────────────────┘
             │
             ↓
    ┌──────────────────────────────────┐
    │  Update WhatsAppMessage           │
    │  - status = "delivered"           │
    │  - deliveredAt = now()            │
    │  - lastStatusAt = now()           │
    └────────┬─────────────────────────┘
             │
             ↓
    ┌──────────────────────────────────┐
    │  Calculate metrics                │
    │  - deliveryTimeMs                 │
    │  - Update aggregated stats        │
    │  - Update per-phone stats         │
    └──────────────────────────────────┘
```

---

## 🔄 Status Transition Diagram

```
┌─────────┐
│ created │
└────┬────┘
     │ sendMessage()
     ↓
 ┌────────┐
 │ queued │ ← Message in Twilio queue
 └────┬───┘
      │ Twilio status: "queued"
      ↓
 ┌────────────┐
 │ sending    │ ← Being sent to carrier
 └────┬───────┘
      │ Twilio status: "sending"
      ↓
 ┌────────────┐
 │ sent       │ ← Delivered to WhatsApp server
 └────┬───────┘
      │ Twilio status: "sent"
      ↓
 ┌────────────┐
 │ delivered  │ ← Received by user's phone
 └────┬───────┘
      │ WhatsApp status: "delivered"
      ↓
 ┌────────────┐
 │ read       │ ← User read message
 └────────────┘
      │
      └─ WhatsApp status: "read"

FAILURE PATH:
     │
     ├─→ failed (error code: 30003-30005, etc.)
     │
     └─→ undelivered (couldn't reach phone)


STATUS CALLBACK FLOW:
    ┌─────────────────────────────────────────┐
    │  Twilio sends status update webhook     │
    │  Every state change:                    │
    │  - queued → sending                     │
    │  - sending → sent                       │
    │  - sent → delivered                     │
    │  - delivered → read                     │
    │  - any → failed                         │
    └─────────────────────────────────────────┘
```

---

## 💾 Database Schema

```
┌──────────────────────────────────────┐
│     WhatsAppMessage Table            │
├──────────────────────────────────────┤
│ id (PK)                              │
│ messageSid (UNIQUE)          ────────┼──→ (from Twilio)
│ from                                 │
│ to                                   │
│ body                                 │
│ direction (INCOMING/OUTGOING)       │
│ status (queued/sent/delivered/read) │
│ errorCode (null or error code)       │
│ sentAt                               │
│ deliveredAt                          │
│ readAt                               │
│ failedAt                             │
│ lastStatusAt                         │
│ createdAt (INDEX)                    │
└──────────────────────────────────────┘
           │
           │ 1:N
           │
           ↓
┌──────────────────────────────────────┐
│  MessageStatusLog Table              │
├──────────────────────────────────────┤
│ id (PK)                              │
│ messageSid (FK)                      │
│ previousStatus                       │
│ newStatus                            │
│ errorCode                            │
│ statusChangedAt                      │
│                                      │
│ Tracks all transitions:              │
│ queued → sending → sent → delivered  │
│ ... queued → failed                  │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│ MessageDeliveryMetrics Table         │
├──────────────────────────────────────┤
│ id (PK)                              │
│ date (INDEX)                         │
│ hour (optional for hourly data)      │
│ totalSent                            │
│ delivered                            │
│ read                                 │
│ failed                               │
│ deliveryRate (%)                     │
│ readRate (%)                         │
│ failureRate (%)                      │
│ averageDeliveryTimeMs                │
│ errorBreakdown (JSON)                │
│  └─ {"30003": 5, "30004": 2}         │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│ PhoneDeliveryPerformance Table       │
├──────────────────────────────────────┤
│ id (PK)                              │
│ phoneNumber (UNIQUE)                 │
│ totalMessages                        │
│ successCount                         │
│ failureCount                         │
│ lastMessageAt                        │
│ deliveryRate (%)                     │
│ healthStatus (healthy/degraded)      │
│ recentErrors (JSON - last 5)         │
└──────────────────────────────────────┘
```

---

## 🔐 Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                          │
└─────────────────────────────────────────────────────────────┘

Layer 1: Transport Security
┌───────────────────────────────────┐
│ HTTPS/TLS                         │
│ - Certificate validation          │
│ - Encrypted in transit            │
│ - No HTTP allowed                 │
└───────────────────────────────────┘
         ↓
Layer 2: Twilio Signature Verification
┌───────────────────────────────────┐
│ HMAC-SHA1 Request Validation      │
│ - Headers: X-Twilio-Signature     │
│ - Verifies: request came from     │
│   legitimate Twilio servers       │
│ - Replay attack prevention        │
│ - Clock skew tolerance: 30s       │
│ - Nonce window: 5min              │
└───────────────────────────────────┘
         ↓
Layer 3: Rate Limiting
┌───────────────────────────────────┐
│ Request Rate Limiting             │
│ - Webhook: 200 req/min per IP    │
│ - API: 1000 req/min per token    │
│ - Burst protection                │
│ - Per-endpoint limits             │
└───────────────────────────────────┘
         ↓
Layer 4: Deduplication
┌───────────────────────────────────┐
│ Message Deduplication             │
│ - MessageSid tracking             │
│ - Prevents double-processing      │
│ - Twilio retries handled          │
└───────────────────────────────────┘
         ↓
Layer 5: Idempotency
┌───────────────────────────────────┐
│ Idempotency Keys                  │
│ - X-Idempotency-Key header        │
│ - Prevents duplicate API calls    │
│ - Caches responses for retries    │
└───────────────────────────────────┘
         ↓
Layer 6: Authentication
┌───────────────────────────────────┐
│ JWT Token Validation              │
│ - Bearer token in Authorization   │
│ - Validates signature             │
│ - Checks expiration               │
└───────────────────────────────────┘
         ↓
Layer 7: Database Security
┌───────────────────────────────────┐
│ Database Access Control           │
│ - PostgreSQL user permissions     │
│ - Connection from app server only │
│ - Encrypted password storage      │
│ - SSL/TLS connection to database  │
└───────────────────────────────────┘
         ↓
Layer 8: Secrets Management
┌───────────────────────────────────┐
│ Secrets Security                  │
│ - Auth tokens in secure vault     │
│ - Quarterly rotation              │
│ - Never logged or exposed         │
│ - Environment-based (not in Git)  │
└───────────────────────────────────┘
```

---

## 📈 Scaling Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                    SCALING STRATEGY                          │
└──────────────────────────────────────────────────────────────┘

As Throughput Increases:

LOW VOLUME (<100 msg/min)
    Single instance: ✅
    Single worker: ✅
    Shared database: ✅
    Local Redis: ✅

MEDIUM VOLUME (100-1000 msg/min)
    Multiple instances: ✅ (3+)
    Multiple workers: ✅ (5-10)
    Dedicated database: ✅
    Managed Redis cluster: ✅

HIGH VOLUME (1000+ msg/min)
    Auto-scaling: ✅ (Kubernetes/Docker)
    Worker pool: ✅ (10-50)
    Database replication: ✅
    Redis cluster: ✅
    Message partitioning: ✅

MASSIVE VOLUME (10,000+ msg/min)
    Distributed system: ✅
    Horizontal sharding: ✅
    Multi-region: ✅
    CDN for metrics: ✅
    Dedicated infrastructure: ✅


Example Kubernetes Deployment:

┌────────────────────────────────┐
│   Load Balancer (HTTPS)        │
└────────────┬───────────────────┘
             │
    ┌────────┴────────┬─────────┐
    ↓                 ↓         ↓
┌─────────┐    ┌─────────┐  ┌─────────┐
│ Pod 1   │    │ Pod 2   │  │ Pod N   │
│ App +   │    │ App +   │  │ App +   │
│ Worker  │    │ Worker  │  │ Worker  │
└────┬────┘    └────┬────┘  └────┬────┘
     │              │            │
     └──────────────┼────────────┘
                    │
        ┌───────────┴───────────┐
        ↓                       ↓
    ┌────────┐           ┌────────┐
    │ Redis  │           │ Postgres│
    │ Cluster│           │ Cluster │
    └────────┘           └────────┘
```

---

## 🎯 Production Checklist Flowchart

```
START
  │
  ├─→ Credentials Ready? ──NO─→ Get from Twilio
  │        │
  │       YES
  │        ↓
  ├─→ HTTPS Working? ──NO─→ Install certificate
  │        │
  │       YES
  │        ↓
  ├─→ Code Integrated? ──NO─→ Add delivery service
  │        │
  │       YES
  │        ↓
  ├─→ DB Migrated? ──NO─→ Run migrations
  │        │
  │       YES
  │        ↓
  ├─→ App Deployed? ──NO─→ Deploy to production
  │        │
  │       YES
  │        ↓
  ├─→ Webhooks Configured? ──NO─→ Update Twilio console
  │        │
  │       YES
  │        ↓
  ├─→ Test Message Sent? ──NO─→ Send test via API
  │        │
  │       YES
  │        ↓
  ├─→ Status Callbacks Received? ──NO─→ Check configuration
  │        │
  │       YES
  │        ↓
  ├─→ Metrics Visible? ──NO─→ Check database
  │        │
  │       YES
  │        ↓
  ├─→ Monitoring Active? ──NO─→ Set up monitoring
  │        │
  │       YES
  │        ↓
  ├─→ Team Trained? ──NO─→ Conduct training
  │        │
  │       YES
  │        ↓
  ├─→ Go-Live Approved? ──NO─→ Get approval
  │        │
  │       YES
  │        ↓
  └──→ LAUNCH! 🚀
```

---

**Generated**: January 22, 2026  
**Status**: ✅ PRODUCTION READY
